<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.CustomTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="coloredButtons" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/AdminDarkBlue</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlHighlight">@color/lightGray</item>
    </style>

    <style name="AdminStatusBar">
        <item name="android:statusBarColor">@color/AdminBlue</item>
    </style>

    <style name="AdminRatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/darkGray</item>
        <item name="colorControlActivated">@color/AdminDarkBlue</item>
    </style>

    <style name="UserRatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/darkGray</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
    </style>

    <style name="LocationProfileEdge">
        <item name="android:colorEdgeEffect">@color/white</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/AdminDarkBlue</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/AdminDarkBlue</item>
        <item name="colorControlNormal">@color/AdminDarkBlue</item>
        <item name="colorControlActivated">@color/AdminDarkBlue</item>

    </style>

</resources>
